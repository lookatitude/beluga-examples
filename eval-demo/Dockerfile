# syntax=docker/dockerfile:1.7
# Multi-stage Dockerfile for eval-demo.
# Generated by `beluga init` on 2026-04-22T01:32:33Z.

FROM golang:1.25-alpine AS builder
WORKDIR /src
COPY go.mod go.sum* ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o /out/eval-demo .

# NOTE: this image requires CGO_ENABLED=0. If you add a package with CGo
# requirements, switch to gcr.io/distroless/base-debian12 or a glibc-based
# runtime image.
FROM gcr.io/distroless/static-debian12:nonroot

LABEL org.opencontainers.image.source="https://example.com/eval-demo"
LABEL org.opencontainers.image.version="v2.13.0"
LABEL org.opencontainers.image.title="eval-demo"

USER nonroot:nonroot
COPY --from=builder /out/eval-demo /eval-demo
ENTRYPOINT ["/eval-demo"]
